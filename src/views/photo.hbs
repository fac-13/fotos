<section>

    <h1>{{photo.title}}</h1>
    <img src={{photo.image_url}} alt="{{photo.title}}" />
    <p>Description: {{photo.description}}</p>

    <p>Username posted: {{photo.username}}</p>
    <p>Date posted: {{photo.date}}</p>

</section>